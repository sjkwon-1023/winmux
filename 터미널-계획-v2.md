# Windows용 경량 cmux 스타일 터미널 — 계획 v2

# 1. 제품 목표

범용 터미널 에뮬레이터를 새로 만드는 것이 아니라, macOS에서 사용 중인 cmux의 핵심 워크플로를 Windows ARM 환경에서 거의 그대로 재현한다.

기본 원칙:

- Windows 11 ARM64 우선
- 모든 셸/개발 명령은 WSL2 Ubuntu에서 실행
- 사용자 설정 최소화
- Claude Code / Codex 중심
- 워크스페이스 중심 UX — 사이드바만 훑어도 어느 에이전트가 입력을 기다리는지 보인다
- pane 분할 + pane 내부 탭
- 패널 간 텍스트/프롬프트 전달
- 에이전트 완료 알림
- Git 브랜치 표시
- 종료 후 프로세스가 아니라 레이아웃만 복원
- 향후 브라우저 탭·에이전트 브라우저 제어·앱 관리 에이전트로 확장 가능

성능 목표:

- 앱 자체 RAM 목표: **100MB 이하**
- 허용 상한: **150MB 이하**
- 측정 기준: **Private Working Set(또는 private commit)**, WebView2 프로세스 트리 전체 포함. 공유 DLL로 부풀려지는 일반 working set 기준을 쓰지 않는다. 판정 흔들림을 막기 위해 이 정의를 고정한다.
- 150MB를 지속적으로 초과하면 렌더링 기술 재검토 (→ 후보 B)
- Idle CPU: 사실상 0%
- 숨겨진 워크스페이스/pane/탭의 불필요한 렌더링 중단
- 메모리 누수 없이 8시간 이상 실행 가능

WSL, Claude Code, Codex, 개발 서버가 사용하는 메모리는 앱 메모리 목표에서 제외한다.

참고: WebView2는 단일 인스턴스여도 browser/GPU/renderer/utility 프로세스 트리를 띄우므로 그것만으로 80~150MB대가 흔하다. 현실적 관문은 100MB 목표가 아니라 150MB 상한이다.

---

# 2. 기술 결정 원칙

프레임워크를 먼저 고정하지 않는다. 터미널 렌더링 후보를 먼저 검증하고, 통과한 렌더러에 맞춰 최종 프레임워크를 결정한다.

## 후보 A — Tauri + 단일 WebView2 + xterm.js (기본 경로)

구조:

- Tauri → WebView2 **한 개** → 전체 GUI → 여러 xterm.js 인스턴스
- Rust backend → ConPTY → wsl.exe → Ubuntu bash → Claude Code / Codex

원칙:

- pane/워크스페이스마다 WebView2를 만들지 않는다. 앱 전체에서 WebView2는 하나.
- 터미널 출력 전달은 Tauri 이벤트(JSON 직렬화)가 아니라 **Tauri v2 raw channel(ArrayBuffer)** 을 사용한다.
- **Flow control 필수**: `yes` 수준 고속 출력 시 Rust→WebView IPC 큐가 무한정 쌓이는 것이 이 조합의 대표적 RAM 폭발 경로다. xterm.write() 완료 콜백 기반 backpressure(큐가 임계치를 넘으면 PTY 읽기 일시 중지)를 처음부터 설계에 포함한다.
- WebView 기반의 부가 이점: 폴더 브라우저·텍스트 뷰어·마크다운 렌더러 탭(6장)을 저비용으로 구현할 수 있다.

채택 조건:

- 터미널 1개 idle RAM ≤ 약 100MB
- 터미널 4개 idle RAM ≤ 150MB
- Claude Code TUI 정상 동작
- 한글 IME 정상
- Ctrl+C / 복사 / 붙여넣기 / resize 정상
- 대량 출력에서 flow control 동작, hang 없음
- **ConPTY의 OSC 7 / 9 / 777 passthrough 확인** (아래 "단일 실패점" 참고)
- ARM64 빌드 가능
- 장시간 메모리 누수 없음

### 단일 실패점: ConPTY OSC passthrough

에이전트 알림(9장)과 cwd 추적(10장)이 모두 "WSL 안에서 출력한 OSC 시퀀스가 앱까지 도달한다"는 가정 위에 있다. ConPTY는 출력을 파싱해 재구성하는 구조라 인식하지 못하는 시퀀스를 잘라먹은 이력이 있다(OSC 8, OSC 52 등이 나중에야 passthrough 지원).

- Spike에서 대상 Windows 빌드 기준으로 OSC 7 / 9 / 777이 실제로 살아서 나오는지 **최우선 검증**한다.
- 잘리는 경우의 대안: hook이 파일 또는 UNIX 소켓에 쓰고 Rust 쪽에서 감시. (v1 "IPC 서버 없음" 원칙과 충돌하지만 OSC가 막히면 불가피)
- OSC 감지는 xterm.js가 아니라 **Rust PTY 리더 단**에서 수행한다. 숨겨진 워크스페이스에서도 동작해야 하고, 시퀀스를 프론트엔드까지 넘길 필요도 없어진다.

## 후보 B — alacritty_terminal + 자체 얇은 렌더러 + TSF

후보 A가 최적화 후에도 150MB를 지속 초과하는 경우에만 진입한다.

(v1 계획의 "Windows Terminal rendering/control 이식" 경로는 폐기한다. Windows Terminal의 렌더링/컨트롤은 외부 앱용 SDK로 배포된 적이 없어, 오픈소스 코드를 vendoring하고 내부 API 변화를 계속 따라가는 지속 유지보수 부채가 된다.)

구조:

- Rust 앱 (UI 포함 전체 네이티브 — 터미널만 네이티브로 빼고 UI를 WebView에 남기면 WebView2 프로세스 트리 비용이 그대로 남아 총합이 줄지 않는다)
- alacritty_terminal 크레이트: VT/ANSI 파싱, 그리드, 스크롤백, selection 로직
- 자체 구현: D3D11(또는 소프트웨어) 글리프 렌더러, **TSF 기반 한글 IME**, 클립보드, DPI, 접근성

기대 수치: private commit 기준 4패널 30~70MB. (레퍼런스: mintty 10~20MB, foot ~10MB, Alacritty 30~60MB — 터미널 자체는 본질적으로 가벼운 물건이며, 100MB를 넘기는 건 런타임 비용이다.)

주의:

- C#/.NET 경로는 런타임만으로 30~60MB가 깔리므로 배제 (또는 NativeAOT 한정)
- 스크롤백은 고정폭 그리드 나이브 저장 금지 (10,000줄 × 200열 × 16B ≈ 패널당 30MB). 가변 길이 행 저장 또는 압축으로 패널당 1~5MB 수준을 목표
- 글리프 아틀라스는 한글/한자로 수천 글리프까지 커질 수 있으므로 LRU 방출로 상한 설정
- ARM 기기는 통합 메모리라 GPU 버퍼도 시스템 RAM을 먹는다 — 측정 시 포함 여부 명시
- 가장 큰 비용은 렌더러가 아니라 IME·클립보드·DPI·접근성 등 "잡일" 층이다

## 제외 — 완전 자체 터미널 렌더러

v1에서 VT parser, glyph rendering, font shaping, Unicode width, IME, cursor, scrollback engine, selection, mouse reporting을 밑바닥부터 직접 구현하지 않는다. 후보 B로 가더라도 파싱/그리드는 alacritty_terminal에 위임한다.

---

# 3. 1단계: 기술 검증 Spike

본 개발 전에 아주 작은 테스트 앱(창 하나, 터미널 하나, 상태 표시 숫자)을 만든다.

구성: xterm.js + Rust PTY + ConPTY + wsl.exe + Ubuntu bash + Claude Code.

## 사전 확인 (Spike 시작 전)

- **Codex CLI의 Linux ARM64 바이너리 존재 여부** — WSL이 ARM64 Ubuntu이므로 x64 바이너리는 동작하지 않는다.

## 기능 검증

- Claude Code 실행, 권한 선택 화면
- Codex 실행
- 방향키, Ctrl+C, Ctrl+D
- 복사, 붙여넣기
- 한글 입력 (조합 중 표시 포함)
- 여러 줄 프롬프트
- 창 resize, full-screen/TUI 갱신
- ANSI 색상, Unicode, Emoji

## OSC passthrough 검증 (필수)

- WSL bash에서 OSC 777 출력 → Rust PTY 리더에서 감지되는지
- OSC 9, OSC 7 동일 확인
- 우선순위: **OSC 777 > 9 > 7** (777은 title+body를 담을 수 있어 사이드바 메시지 미리보기의 근거가 된다)
- 잘리는 경우 파일/소켓 기반 대안 경로의 프로토타입까지 확인

## 렌더러 비교 (필수)

- xterm.js **DOM 렌더러와 WebGL addon을 둘 다** 측정한다. DOM 렌더러는 대량 출력에서 병목이 가장 심해 "대량 출력 안정" 기준을 통과하지 못할 수 있다. WebGL은 CPU를 줄이는 대신 GPU 메모리를 쓰므로 ARM 통합 메모리 특성과 함께 판단한다.

## Flow control 검증

- `yes` 수준 고속 출력에서 IPC 큐 상한 + PTY 읽기 일시 중지가 동작하는지
- backpressure 없이/있이 RAM 비교

## 키보드 가로채기 목록

- 앱이 가로채는 단축키 목록을 명시적으로 작성하고, 터미널로 가야 할 Ctrl 조합(Ctrl+C 등)과의 충돌을 IME 검증과 같은 단계에서 확인한다. (cmux의 Cmd → Windows Ctrl 치환 시 충돌 발생 지점)

## 부하

- `yes` 수준의 빠른 출력, 수 MB 로그 출력
- 5,000줄 / 10,000줄 scrollback
- 터미널 4개 / 8개 동시 실행
- **teardown→재구성 지연**: replay buffer 재생으로 터미널을 재구성하는 데 걸리는 시간 (버퍼 크기별, 목표 100ms급 — 워크스페이스 전환 체감의 근거)

## 리소스 측정

상태: ① 앱만 ② WSL 터미널 1개 ③ 4개 ④ 8개 ⑤ 4개 + Claude Code 2개 ⑥ 백그라운드 워크스페이스 포함

항목: private working set(WebView2 트리 포함), idle CPU, GPU, handle/thread 증가, 2시간/8시간 후 RAM

판정:

- 4패널 ≤100MB: 매우 좋음
- 100~150MB: 채택
- >150MB: 최적화
- 최적화 후에도 >150MB: 후보 B 진입

---

# 4. 애플리케이션 데이터 모델

UI와 독립적으로 상태 모델을 먼저 만든다.

## 계층 구조 (v2에서 변경)

```
Workspace
└─ Split Tree (워크스페이스당 레이아웃 1개)
   └─ Pane (= TabContainer)
      └─ Tab (여러 개, 타입 다양)
```

워크스페이스 레벨 탭은 **없다.** 탭 층은 pane 내부 하나뿐이다. (두 층의 탭은 "지금 뭘 닫는 건지" 헷갈리는 안티패턴. "전체 화면 컨텍스트 여러 개"가 필요하면 pane 하나짜리 레이아웃에 탭 여러 개로 커버한다.) VS Code 에디터 그룹 / Zellij tabbed container와 같은 모델이며, cmux 실제 UX와 일치한다.

## Workspace

- id
- name
- rootPath — 워크스페이스의 정체성. 같은 리포라도 backend/frontend를 별개 워크스페이스로 둘 수 있다. 새 탭/새 pane의 기본 cwd.
- distro — 이 워크스페이스의 터미널이 연결될 WSL 배포판. 격리 배포판 지정에 사용 (5장).
- layoutRoot (split tree)
- activePaneId
- **agentStatus**: `running | needsInput | idle` — 이진 unread가 아닌 3값 enum
- lastAgentMessage — 사이드바 미리보기용 (OSC 777 body)
- gitBranch, gitDirty
- ports — v2 (자리만 예약)

## Pane (TabContainer)

- id — **세션 내 안정적이고 외부에서 참조 가능한 ID** (향후 MCP 도구의 대상 지정에 사용)
- tabs[]
- activeTabId

## Tab

- id — 동일하게 안정 ID
- **type**: `terminal | folderBrowser | textViewer | markdownViewer` (v2: `browser`)
- title
- notificationState
- lastActivity
- 타입별 상태:
  - terminal: cwd, ptySessionId, terminalState
  - folderBrowser: path
  - textViewer / markdownViewer: filePath, 스크롤 위치
  - browser(v2): url, 히스토리

## Split Tree

- PaneNode
- SplitNode: direction, ratio, first, second

이 모델은 UI 기술과 무관하게 유지한다.

## Command Dispatcher (v1부터 필수)

UI의 모든 조작(탭 생성/닫기, 분할, 포커스 이동, 텍스트 전달, 파일 열기)은 **단일 command dispatcher를 경유**한다. 키보드 단축키·마우스·(향후) MCP 도구가 전부 같은 command bus를 호출한다. 지금 비용은 거의 0이고, v2의 관리 에이전트(15장)에서 MCP 서버가 이 bus를 노출하는 얇은 층이 된다.

---

# 5. WSL 실행 모델

앱에서 Windows shell을 제공하지 않는다.

새 터미널 탭 생성: ConPTY → wsl.exe → 워크스페이스에 지정된 배포판 → bash login shell

- 기본 작업 경로는 WSL Linux filesystem (`/home/<user>/projects/...`). `/mnt/c/...` 비권장.
- PowerShell/CMD 프로필 선택 UI는 v1에 없다.
- 기본 배포판은 Windows interop 유지 (explorer.exe, 브라우저, Windows helper 호출 가능).

## 격리 배포판 (WSL 외부 접근 차단)

터미널 앱 층에서의 명령 필터링은 하지 않는다. 앱은 바이트를 PTY에 전달할 뿐이라 앱 레벨 필터는 스크립트/alias 한 번에 우회되고 정상 입력만 깨뜨린다. 앱이 하는 것은 Windows shell을 아예 제공하지 않는 것까지이고, 실제 차단은 WSL 배포판 설정(`/etc/wsl.conf`)이 담당한다:

- `[interop] enabled=false` — WSL 내에서 Windows 실행파일(cmd.exe, powershell.exe 등) 실행 차단
- `[interop] appendWindowsPath=false` — Windows PATH 혼입 차단
- `[automount] enabled=false` — `/mnt/c` 마운트 제거, Windows 파일시스템 접근 차단

방향 주의: 이 설정들은 **WSL→Windows 방향**만 막는다. 앱의 파일 뷰어는 **Windows→WSL 방향**(`\\wsl.localhost`)으로 읽으므로 완전히 잠근 배포판에서도 폴더 브라우저·뷰어 탭은 정상 동작한다.

운용 방식:

- 전역으로 interop을 끄지 않는다 (explorer.exe 호출 등 편의 상실).
- 대신 **에이전트 작업용 격리 배포판을 별도로 두고**, Workspace의 `distro` 필드로 워크스페이스별 연결한다. 일반 워크스페이스는 interop 켜진 기본 배포판, Claude Code/Codex 중심 워크스페이스는 격리 배포판.
- 격리 배포판에서는 에이전트가 `C:\`를 건드릴 경로 자체가 존재하지 않는다.
- v1: `distro` 필드 지원 (wsl.exe `-d` 인자 — 비용 거의 없음). 격리 배포판 생성/설정을 대신 해주는 온보딩 프로비저닝은 v2.

---

# 6. UX 구조

cmux와 동일한 3층 모델: **워크스페이스 = 큰 단위 작업 관리, pane = 화면 분할, 탭 = 같은 자리의 컨텍스트 전환.**

## 화면 구조

- 왼쪽: Workspace sidebar (상태 카드)
- 중앙: 활성 워크스페이스의 split panes
- 각 pane 상단: 탭 바 + pane 조작 아이콘

## 사이드바 = 상태 카드

각 워크스페이스 항목이 표시하는 것:

- 이름
- 상태: ⚡Running / 🔔Needs input / Idle (agentStatus)
- 마지막 에이전트 메시지 미리보기 1~2줄 (OSC 777 body)
- 브랜치 + dirty 마커 (`main*`)
- 경로 (축약)
- 포트 (v2 — WSL 내 `ss -tlnp`류 폴링이 필요해 "폴링 최소화" 원칙과 충돌하므로 v1 제외, 카드 레이아웃에 자리만 예약)

사이드바만 훑어도 어느 에이전트가 입력을 기다리는지 보이는 것이 이 UX의 핵심이다.

## pane 헤더

우상단 아이콘 4개로 pane 단위 조작을 한정한다:

- 새 터미널 탭
- 새 브라우저 탭 (v1에서는 숨김/비활성)
- 좌우 분할
- 상하 분할

## 탭 타입별 동작

### folderBrowser / textViewer / markdownViewer

- 파일 접근은 Rust 쪽에서 `\\wsl.localhost\Ubuntu\...` 경로로. 단일 파일 읽기는 충분히 빠르다.
- **9P 한계**: WSL 내 파일 변경(inotify)이 Windows로 전파되지 않는다. 마크다운 라이브 리로드는 활성 탭일 때만 짧은 주기의 mtime 폴링으로 타협.
- textViewer는 **청크 로딩 + 가상 스크롤 필수.** 수백 MB 로그를 통째로 올리면 메모리 예산이 즉시 깨진다.
- **뷰어이지 에디터가 아니다.** 편집이 들어가는 순간 저장 충돌·undo·인코딩 등 별개 프로젝트가 된다. 이 선을 유지한다.
- 비활성 탭은 DOM을 유지하지 않고 **상태만 남기고 unmount** (스크롤 위치, 경로만 저장). 터미널 탭만 예외 — xterm 인스턴스는 유지하되 렌더링만 중단.

### browser (v2)

별도 BrowserPane이 아니라 **탭 타입**이다. 주소창·뒤로/앞으로·스크린샷 버튼이 탭 컨텐츠 상단에 붙는다. 임의 사이트 렌더링은 iframe 제약 때문에 child webview가 필요하며, 이는 단일 WebView 원칙의 예외이므로 **v2 진입 시 메모리 예산 재산정 항목**으로 표시해 둔다.

## 키보드 모델

3층 구조에 맞춘 3종 이동:

- 워크스페이스 전환: Ctrl+1~9 또는 Ctrl+↑↓
- pane 포커스 이동: Alt+방향키 류
- pane 내 탭 순환: Ctrl+Tab

cmux 단축키를 최대한 유사하게 매핑하되, 터미널로 가야 할 Ctrl 조합과의 충돌은 Spike의 "가로채기 목록"으로 관리한다. 사용자 단축키 설정은 후순위.

---

# 7. 탭 및 분할 기능 범위

지원:

- pane 내 새 탭 / 탭 닫기 / 탭 전환
- 좌우 분할 / 상하 분할
- pane 닫기 (마지막 탭 닫힘 시 pane 정리 규칙 포함)
- splitter resize
- active pane 전환

v1 제외:

- 탭 drag-and-drop (pane 간 이동 포함)
- pane detach / 별도 window

---

# 8. 패널 간 텍스트 전달

단순 `PTY.write(text)`로 구현하지 않는다. API를 셋으로 나눈다:

- **sendText** — 텍스트만 전달, 실행하지 않음
- **sendTextAndSubmit** — 텍스트 전달 후 `\r`
- **sendRaw** — Ctrl+C, Escape, 방향키 등 특수 키

구현 주의:

- Bracketed paste 시퀀스(`ESC[200~`)를 PTY에 raw로 직접 쓰지 않는다. 수신 앱이 paste mode를 켜지 않았으면 이스케이프 시퀀스가 입력으로 그대로 들어간다. **xterm.js의 paste 경로(현재 paste mode 상태를 추적)를 경유**해 구현한다.
- 기본 UI에서 "전달"과 "전달 후 실행"을 분리해 실수 실행을 막는다.
- 이 세 API는 그대로 v2 MCP 도구(15장)의 기반이 된다.

---

# 9. 에이전트 상태 및 알림

v1에서는 IPC 서버, Named Pipe, Windows helper CLI를 만들지 않는다. PTY 출력 자체를 이용한다.

경로:

Claude Code hook → 현재 TTY에 OSC 출력 (hook stdout은 Claude Code가 소비하므로 `> /dev/tty` 리다이렉트) → **Rust PTY 리더에서 감지** → 해당 탭/pane/워크스페이스 상태 갱신

- 우선 사용: **OSC 777** (title + body → 사이드바 메시지 미리보기). 불가 시 OSC 9.
- BEL만을 완료 신호로 쓰지 않는다.
- ConPTY가 시퀀스를 잘라먹으면 파일/소켓 감시 대안으로 전환 (2장 참조).

상태 라우팅 (탭 층 추가 반영):

- Tab: notificationState
- Pane: 탭 배지 집계 → 테두리 또는 ● 표시
- Workspace: agentStatus (`running | needsInput | idle`) + 메시지 미리보기 → 사이드바 카드
- 선택: Windows toast
- 사용자가 해당 탭을 활성화하면 unread 해제

v2: Windows helper CLI, Named Pipe, 세부 payload, agent 상태 API.

---

# 10. Git 정보

MVP 핵심 실행 경로 안정화 후 추가.

v1 최소 지원: 현재 branch, dirty 여부, repository root — 표시 위치는 **사이드바 카드**.

실행 방식 (필수):

- **git은 반드시 WSL 안에서 실행한다**: `wsl.exe -d Ubuntu -- git -C <path> ...`
- WSL 파일시스템의 repo를 Windows 쪽에서 `\\wsl$` 경유로 조회하면 성능이 처참하다.

갱신 시점: workspace 활성화, pane focus, cwd 변경, 사용자 refresh. 매초 폴링하지 않는다.

경로 추적은 OSC 7 shell integration 우선 (passthrough 확인 전제).

v2: ahead/behind, modified count, worktree 목록/생성, PR 정보.

---

# 11. 상태 저장 / 복원

복원 대상:

- Workspace 목록·순서·rootPath
- Split layout, active pane
- 각 pane의 탭 목록·순서·activeTab
- 탭별: type, title, cwd(터미널) / path(뷰어·폴더) / 스크롤 위치
- 기본 실행 command

복원하지 않는 것: 프로세스 메모리 상태, Claude Code 프로세스, 실행 중 서버.

재실행 시: 레이아웃과 탭 구조 복원 → 터미널 탭에 새 WSL shell 생성, 뷰어 탭은 파일 재로드.

Claude/Codex 세션 복원은 v2 — 단, Claude Code의 `--resume`/`--continue` 덕에 난도가 낮으므로 v2 목록 상단에 둔다.

---

# 12. 메모리 최적화 원칙

처음부터 적용한다.

- **WebView 하나만**: pane별/워크스페이스별 WebView 금지. (v2 browser 탭의 child webview만 예외이며 별도 예산)
- **Flow control**: PTY→WebView 전달에 backpressure 필수 (2장). raw channel 사용, JSON 직렬화 금지.
- **inactive 렌더링 중단**: 활성 워크스페이스 내 비활성 pane/탭의 repaint 금지. resize observer 최소화, animation 제거.
- **비활성 뷰어 탭 unmount**: 상태만 저장하고 DOM 해제.
- **Rust replay buffer**: pane별로 최근 PTY 출력을 상한(예: ~1MB) 내에서 Rust에 보관한다. 워크스페이스 teardown/재구성과 WebView 리셋(아래)의 근거 데이터. 8 pane 기준 추가 메모리 ~8MB 수준.
- **숨김 워크스페이스 완전 teardown**: 워크스페이스를 떠날 때 해당 워크스페이스의 xterm 인스턴스와 DOM을 전부 파괴한다 (PTY는 계속 실행). 복귀 시 replay buffer에서 재구성하고, TUI 앱은 SIGWINCH로 재그리기를 유도한다. 숨김 워크스페이스의 WebView 메모리가 0이 되고, 웹 쪽 누수가 쌓일 표면적 자체가 사라진다. 전환 지연 목표: 100ms급 (Spike에서 측정).
- **scrollback 제한**: 기본 **5,000줄**, 10,000줄은 옵션. 무제한 금지. (pane 8개 × 10,000줄이면 버퍼만 수십 MB)
- **addon 최소화**: 시작은 fit + unicode. WebGL은 Spike 측정 결과에 따라 (DOM 렌더러가 대량 출력 기준을 통과 못 하면 WebGL이 기본이 될 수 있다). search 등은 측정 후.
- **React state에 터미널 출력 저장 금지**: PTY output → xterm.write() 직접 전달. text buffer는 xterm.js 소유.
- **textViewer 청크 로딩**: 전체 파일을 메모리에 올리지 않는다.

## WebView 리셋 안전망 (자동 UI 리셋)

PTY 세션·레이아웃·replay buffer 등 모든 영속 상태가 Rust에 있으므로, WebView는 세션 손실 없이 통째로 리로드할 수 있다. 웹 쪽에 어떤 누수가 있어도 원점으로 복구되는 탈출구다.

원칙:

- **UI 버튼은 만들지 않는다.** 리셋은 command dispatcher의 내부 커맨드로만 존재하고(디버깅·향후 MCP용), 아래 자동 트리거들이 이 커맨드를 호출한다.
- **활성 사용 중에는 절대 실행하지 않는다.** 무조건적 주기 타이머 금지.
- 리셋 후 복원: 레이아웃/활성 상태는 Rust 상태에서, 터미널 내용은 replay buffer에서, TUI는 SIGWINCH 재그리기로. 사용자에게 보이지 않아야 한다.

자동 트리거:

1. **Idle 리셋**: 사용자 입력이 일정 시간(예: 30분) 이상 없을 때 1회. 타이머는 idle 상태에서만 카운트.
2. **비가시 리셋**: 창 최소화 또는 포커스 아웃 상태가 일정 시간 지속되면 실행. 가장 안전한 시점.
3. **메모리 워치독**: WebView 프로세스 private 메모리가 임계치를 넘으면 리셋을 예약하고, 다음 안전한 순간(다음 idle 또는 다음 워크스페이스 전환 직후)에 실행. 시간이 아니라 실제 메모리 증가에 반응하는 가장 중요한 트리거.

워크스페이스 전환 시에는 전체 WebView 리셋을 하지 않는다 (전환마다 깜빡임/지연 발생). 전환 시점의 정리는 위의 "숨김 워크스페이스 완전 teardown"이 담당하며, 이것만으로 떠난 워크스페이스의 웹 쪽 리소스는 전부 해제된다.

---

# 13. ARM64 정책

처음부터 x64 + ARM64 CI 빌드.

의존성 추가 전 확인: Windows ARM64 지원, Rust crate ARM64 지원, Node native addon 여부, prebuilt binary 의존 여부. pure Rust dependency 우선.

**Spike 전 확인: Codex CLI Linux ARM64 바이너리 존재 여부** (WSL이 ARM64 Ubuntu이므로).

ARM 실기기 검증: IME, GPU rendering(통합 메모리 — GPU 버퍼도 시스템 RAM 소비, 측정 시 포함 여부 명시), WebView2, battery, sleep/wake, WSL, Claude Code, Codex, 대량 로그.

---

# 14. MVP 범위

## 반드시 포함

- Windows ARM64 대응, WSL 전용
- xterm 터미널 (flow control 포함)
- Workspace sidebar — 상태 카드 (agentStatus, 메시지 미리보기, 브랜치)
- pane 분할 (좌우/상하) + resize
- **pane 내부 탭** (터미널 탭)
- Claude Code / Codex 정상 사용
- sendText / sendTextAndSubmit / sendRaw
- OSC 기반 에이전트 알림 (777 우선)
- layout + 탭 구조 persistence
- current branch + dirty 표시
- command dispatcher + 안정 ID 체계
- 키보드 3층 이동 (워크스페이스/pane/탭)
- 워크스페이스별 WSL 배포판 지정 (`distro` 필드)
- Rust replay buffer + 숨김 워크스페이스 teardown/재구성
- 자동 UI 리셋 (idle / 비가시 / 메모리 워치독 — 버튼 없음)

## 있으면 좋은 것

- folderBrowser 탭
- textViewer 탭
- markdownViewer 탭 (옵션 기능)
- Windows toast
- workspace rename

## 제외

- browser 탭 / browser automation / MCP browser bridge
- 관리 에이전트 (MCP 서버) — 기반만 v1에 포함
- SSH, multiple shell profiles, plugins
- PR 표시, worktree 자동 생성
- 프로세스/세션 복원
- 탭 drag-and-drop, pane detach
- 포트 표시
- 완전 자체 터미널 렌더러

---

# 15. v2

MVP가 일상 사용 수준으로 안정화된 후.

## 세션 복원

- Claude Code `--resume`/`--continue` 연동 (난도 낮음 — 우선순위 상단)

## 격리 배포판 프로비저닝

- 온보딩에서 격리 배포판(interop·automount 꺼진 에이전트용) 생성/설정을 대신 수행
- 워크스페이스 생성 시 "격리 배포판 사용" 토글

## Worktree

- 현재 worktree 표시, 목록, 생성, workspace 연결

## Browser 탭

- Tab type에 `browser` 추가 (child webview)
- 주소창, 히스토리, 스크린샷
- **메모리 예산 재산정 필수**

## 관리 에이전트 (사이드바 에이전트)

커스텀 챗 UI + API 직접 호출로 만들지 않는다. **앱(Rust)이 MCP 서버를 노출**하고, 관리 에이전트는 이 MCP가 연결된 Claude Code 인스턴스 하나로 구성한다. 에이전트 루프·권한 프롬프트·인증을 전부 기존 도구에서 얻는다.

도구 예시:

- `list_workspaces`, `list_panes`, `list_tabs`
- `create_pane`, `create_tab`, `close_tab`
- `read_pane_output(paneId, lines)` — "3번 pane 에러 보고 고쳐줘"를 가능하게 하는 킬러 기능
- `send_text`, `send_text_and_submit` — 8장의 분리를 그대로 유지, submit 계열은 도구 권한 확인에 맡기되 기본값 보수적으로
- `open_file_tab(path)`

구현은 v1의 command dispatcher를 노출하는 얇은 층. v2 브라우저 제어 도구와 같은 MCP 서버에 도구 패밀리 2개로 통합한다.

배치: 사이드바는 좁으므로 대화 UI를 넣지 않는다. 사이드바에는 워크스페이스/알림만 두고, 에이전트는 토글식 우측 드로어 또는 일반 pane의 터미널 탭으로 띄운다.

## Agent Browser Control

Claude Code → MCP → browser bridge → WebView2/CDP: page read, accessibility tree, click, type, navigate, screenshot.

## External Chrome

필요 시: dedicated Chrome profile + CDP, 또는 extension + native messaging.

## 기타

- 포트 표시 (사이드바 카드)
- 알림 세부 payload / agent 상태 API
- scrollback 사용자 설정

---

# 16. 성공 기준

## UX

cmux에서 Windows로 옮겼을 때 작업 습관 변경이 거의 없어야 한다. 워크스페이스=작업 단위, pane=분할, 탭=컨텍스트 전환의 3층 모델이 동일하게 동작하고, 사이드바만으로 에이전트 대기 상태를 파악할 수 있어야 한다.

## 성능 (4 pane 기준)

- RAM 목표 ≤100MB / 상한 ≤150MB (private working set, WebView2 트리 포함)
- idle CPU ≈ 0%
- background pane 불필요 redraw 없음
- 고속 출력에서 flow control 동작, RAM 폭주 없음

## 안정성

- Claude Code / Codex 장시간 사용
- 한글 입력 문제 없음
- resize 문제 없음
- 대량 로그 hang 없음
- 8시간 후 심각한 leak 없음

## ARM64

- native ARM64 build, Prism 없이 네이티브 실행

---

# 17. 개발 순서

1. **사전 확인**: Codex CLI ARM64 바이너리
2. Spike: Tauri + xterm.js + ConPTY + WSL 터미널 1개
3. **ConPTY OSC 777/9/7 passthrough 검증** (실패 시 파일/소켓 대안 프로토타입)
4. Flow control 구현 + 고속 출력 테스트
5. DOM vs WebGL 렌더러 측정
6. RAM/CPU 측정 (private working set 기준)
7. Claude Code + 한글 IME 검증 + **가로채기 키 목록 작성**
8. 터미널 4개 부하 테스트
9. **150MB 판정** — 초과 지속 시에만 후보 B(alacritty_terminal 경로) 진입
10. 데이터 모델 + command dispatcher + 안정 ID
11. pane 분할 (split tree)
12. pane 내부 탭 (터미널 탭)
13. Workspace sidebar (상태 카드)
14. Rust replay buffer + 숨김 워크스페이스 teardown/재구성 (전환 지연 측정)
15. Layout + 탭 persistence
16. 자동 UI 리셋 (idle / 비가시 / 메모리 워치독 트리거)
17. sendText / sendTextAndSubmit / sendRaw
18. OSC 알림 → agentStatus / 미리보기 라우팅
19. Git branch (WSL 내 실행)
20. 키보드 3층 이동
21. folderBrowser / textViewer / markdownViewer 탭
22. ARM64 CI
23. ARM 실기기 테스트

핵심 원칙: **"네이티브가 더 가벼울 것이라는 이유만으로 어려운 경로를 먼저 선택하지 않는다. 실제 수치가 150MB를 지속적으로 넘을 때만 복잡도를 감수하며, 그 경우에도 파싱/그리드는 검증된 크레이트에 위임한다."**
