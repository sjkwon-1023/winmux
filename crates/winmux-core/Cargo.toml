[package]
name = "winmux-core"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
portable-pty = "0.9"
# 상태 모델·command bus 직렬화 계약용. 하한 1.0.186 = rename_all_fields 필요 버전.
serde = { version = "1.0.186", features = ["derive"] }
# persistence(15단계) state.json 읽기/쓰기 — dev-dep 에서 승격 (golden fixture 테스트도 공유).
serde_json = "1"

[dev-dependencies]
# persist 테스트의 임시 디렉터리용
tempfile = "3"
