# CI (stage 22 — 계획 v2 13장 "처음부터 x64 + ARM64 CI 빌드").
#
# 두 job 으로 나눈 이유는 비용이다: gates 는 ubuntu(1x 분 단가)에서 매 push/PR 마다
# 레포의 게이트 5종 + ARM64 타입 체크를 돌리고, 실제 Windows 릴리스 빌드(x64 +
# ARM64 아티팩트)는 2x 단가의 windows 러너라 수동 트리거(workflow_dispatch)와 태그
# 에서만 돈다 — ARM64 실기 테스트(23단계)가 필요할 때 아티팩트를 뽑는 용도다.
#
# clippy 를 ubuntu 에서 Windows 두 타깃으로 돌릴 수 있는 근거: check/clippy 는
# 링크를 하지 않아 MSVC import 라이브러리가 필요 없다 (dev 머신에서 검증된 경로).
# 단 tauri-build 의 리소스 임베드가 llvm-rc 를 찾으므로 apt llvm 이 필요하다.

name: CI

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# 기본은 읽기 전용 — 쓰기가 필요한 job(릴리스 첨부)만 job 단위로 넓힌다.
permissions:
  contents: read

jobs:
  gates:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
          targets: x86_64-pc-windows-msvc,aarch64-pc-windows-msvc

      - uses: Swatinem/rust-cache@v2

      # tauri-build 가 Windows 타깃 check 중 리소스 임베드에 llvm-rc 를 쓴다.
      - name: Install llvm-rc
        run: |
          sudo apt-get update -q
          sudo apt-get install -y -q llvm
          command -v llvm-rc || sudo ln -s "$(ls /usr/bin/llvm-rc-* | head -1)" /usr/local/bin/llvm-rc

      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: |
            apps/winmux/package-lock.json
            apps/spike/package-lock.json

      - name: Core tests
        run: cargo test -p winmux-core

      - name: Clippy (x64 Windows target)
        run: cargo clippy --workspace --all-targets --target x86_64-pc-windows-msvc -- -D warnings

      - name: Clippy (ARM64 Windows target)
        run: cargo clippy --workspace --all-targets --target aarch64-pc-windows-msvc -- -D warnings

      - name: winmux frontend (build + vitest)
        working-directory: apps/winmux
        run: |
          npm ci
          npm run build
          npx vitest run

      - name: spike frontend (build + vitest)
        working-directory: apps/spike
        run: |
          npm ci
          npm run build
          npx vitest run

  windows-artifacts:
    # 2x 단가 러너 — 매 push 가 아니라 수동/태그에서만. ARM64 실기 테스트용
    # 아티팩트 산출이 목적이다 (23단계). 태그 트리거일 때는 추가로 GitHub Release 에 첨부한다.
    if: github.event_name == 'workflow_dispatch' || startsWith(github.ref, 'refs/tags/v')
    runs-on: windows-latest
    # job 단위 최소 권한 — Release 첨부(softprops/action-gh-release)에만 필요, gates job 에는 안 준다.
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        target: [x86_64-pc-windows-msvc, aarch64-pc-windows-msvc]
        include:
          - target: x86_64-pc-windows-msvc
            asset_name: winmux-x64.exe
          - target: aarch64-pc-windows-msvc
            asset_name: winmux-arm64.exe
    steps:
      - uses: actions/checkout@v5

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: apps/winmux/package-lock.json

      - name: Build winmux (release, no bundle)
        working-directory: apps/winmux
        run: |
          npm ci
          npm run tauri build -- --target ${{ matrix.target }} --no-bundle

      - uses: actions/upload-artifact@v4
        with:
          name: winmux-${{ matrix.target }}
          # 워크스페이스 루트 target 디렉터리 (레포 루트가 cargo 워크스페이스다).
          path: target/${{ matrix.target }}/release/winmux-app.exe
          if-no-files-found: error

      # 태그 트리거 전용: Release 에 올릴 타깃별 명확한 이름의 복사본.
      # workflow_dispatch 실행은 위 artifact 업로드(원본 파일명)만 하고 여기서 끝난다.
      - name: Copy release asset
        if: startsWith(github.ref, 'refs/tags/v')
        run: cp target/${{ matrix.target }}/release/winmux-app.exe target/${{ matrix.target }}/release/${{ matrix.asset_name }}

      - name: Attach to GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: target/${{ matrix.target }}/release/${{ matrix.asset_name }}
