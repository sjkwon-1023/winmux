{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window capability — core APIs (event listen for state-changed/osc-event). Without this the ACL rejects listen() and the whole frontend dies (spike 회귀 교훈).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
